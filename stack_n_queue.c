#include "library.h"
/**
 * is_stack - adds the top two elements of the stack
 * @head: pointer to top
 * @number: integer
 */
void is_stack(sstack_t **head, unsigned int number)
{
	head = head;
	number = number;
	numbers[1] = 0;
}
/**
 * is_queue - adds the top two elements of the stack
 * @head: pointer to top
 * @number: integer
 */
void is_queue(sstack_t **head, unsigned int number)
{
	number = number;
	head = head;
	numbers[1] = 1;
}
